/**
 * @file transfer-zones.ts
 * @description Backward-compatible barrel re-export for the transfers domain.
 * All domain logic, zones, destinations, vehicles, and pricing formulas are modularized under `@/lib/transfers`.
 */

export * from "./transfers";
